# Additional Python dependencies for Voxtral vLLM container
# IMPORTANT: Do NOT add packages that could upgrade torch/torchvision/vllm —
# this breaks the base image's carefully matched CUDA/torch/vLLM stack.
# All core deps (fastapi, uvicorn, openai, requests, pydantic) are already
# in the vLLM base image. Only list packages that model.py actually imports
# and that are NOT already in the base image or Dockerfile.

# (Currently empty — all needed packages are installed in the Dockerfile)